<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <ol>
    {{#each data}}
    {{#if devoured}}
    <li style="text-decoration:line-through">{{burger_name}}</li>
    {{else}}
    <li>{{burger_name}}</li>
    {{/if}}
    {{/each}}
    </ol>
</body>
</html>